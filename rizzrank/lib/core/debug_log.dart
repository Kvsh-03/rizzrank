/// Debug logging stub — no-op in production.
void debugLog({
  required String location,
  required String message,
  Map<String, dynamic>? data,
  String? hypothesisId,
}) {
  // Intentionally empty — remove all callers over time.
}
